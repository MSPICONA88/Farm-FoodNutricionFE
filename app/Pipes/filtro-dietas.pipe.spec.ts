import { FiltroDietasPipe } from './filtro-dietas.pipe';

describe('FiltroDietasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDietasPipe();
    expect(pipe).toBeTruthy();
  });
});
