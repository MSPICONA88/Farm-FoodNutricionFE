import { FiltroTratPipe } from './filtro-trat.pipe';

describe('FiltroTratPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTratPipe();
    expect(pipe).toBeTruthy();
  });
});
